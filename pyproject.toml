[project]
name = "udacity-course"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "openai>=2.38.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.14",
]
